# 订单模块接口代码说明

## API 列表

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/orders/submit` | 登录用户 | 从购物车提交订单 |
| GET | `/api/orders/my?page=1&size=10` | 登录用户 | 分页查询自己的订单 |
| GET | `/api/orders/{id}` | 登录用户/管理员 | 订单详情（含明细） |
| GET | `/api/orders/admin?page=1&size=10` | 管理员 | 分页查询所有订单 |
| PUT | `/api/orders/{id}/status` | 管理员 | 更新订单状态 |

## 统一返回格式

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [...],
    "total": 50,
    "size": 10,
    "current": 1
  }
}
```

## 核心代码

### Service 接口

```java
public interface OrderService {
    ShopOrder submitFromCart();
    Page<ShopOrder> myOrders(int page, int size);
    ShopOrder detail(Long id);
    Page<ShopOrder> adminList(int page, int size);
    void updateStatus(Long id, String status);
}
```

### Mapper（继承 BaseMapper）

```java
@Mapper
public interface ShopOrderMapper extends BaseMapper<ShopOrder> {
    @Select("SELECT COUNT(1) FROM shop_order o ...")
    int countPurchasedByUserAndProduct(
        @Param("userId") Long userId,
        @Param("productId") Long productId);
}
```

### 分页查询示例（myOrders）

```java
public Page<ShopOrder> myOrders(int page, int size) {
    Long userId = UserContext.currentUserId();
    Page<ShopOrder> p = shopOrderMapper.selectPage(
        new Page<>(page, size),
        new LambdaQueryWrapper<ShopOrder>()
            .eq(ShopOrder::getUserId, userId)
            .orderByDesc(ShopOrder::getId));
    for (ShopOrder o : p.getRecords()) {
        o.setItems(orderItemMapper.selectList(
            new LambdaQueryWrapper<OrderItem>()
                .eq(OrderItem::getOrderId, o.getId())));
    }
    return p;
}
```

### 下单流程（事务）

```java
@Override
@Transactional(rollbackFor = Exception.class)
public ShopOrder submitFromCart() {
    // 1. 查询购物车
    // 2. 遍历校验库存
    // 3. 逐行扣减库存（带行锁条件）
    // 4. 计算总金额
    // 5. 插入订单主表
    // 6. 批量插入订单明细
    // 7. 清空购物车
    // 8. 返回完整订单
}
```

## 文件清单

| 文件 | 职责 |
|------|------|
| `entity/ShopOrder.java` | 订单实体，`@TableName("shop_order")` |
| `entity/OrderItem.java` | 订单明细实体 |
| `mapper/ShopOrderMapper.java` | 继承 `BaseMapper<ShopOrder>` |
| `mapper/OrderItemMapper.java` | 继承 `BaseMapper<OrderItem>` |
| `service/OrderService.java` | 订单服务接口 |
| `service/impl/OrderServiceImpl.java` | 订单服务实现（构造器注入） |
| `controller/OrderController.java` | REST API（构造器注入） |
