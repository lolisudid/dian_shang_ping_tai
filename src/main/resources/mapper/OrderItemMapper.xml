<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.OrderItemMapper">

    <select id="listByOrderId" resultType="com.ecommerce.entity.OrderItem">
        SELECT id, order_id AS orderId, product_id AS productId, product_name AS productName,
               price, quantity
        FROM order_item WHERE order_id = #{orderId}
    </select>

    <insert id="insertBatch">
        INSERT INTO order_item (order_id, product_id, product_name, price, quantity) VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>
</mapper>
