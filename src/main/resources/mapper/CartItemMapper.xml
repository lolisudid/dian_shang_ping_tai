<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.CartItemMapper">

    <select id="listByUserId" resultType="com.ecommerce.entity.CartItem">
        SELECT c.id, c.user_id AS userId, c.product_id AS productId, c.quantity, c.create_time AS createTime,
               p.name AS productName, p.price AS productPrice, p.image_url AS productImageUrl, p.stock AS productStock
        FROM cart_item c
        INNER JOIN product p ON c.product_id = p.id AND p.deleted = 0
        WHERE c.user_id = #{userId}
        ORDER BY c.id DESC
    </select>

    <select id="findByUserAndProduct" resultType="com.ecommerce.entity.CartItem">
        SELECT id, user_id AS userId, product_id AS productId, quantity, create_time AS createTime
        FROM cart_item WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_item (user_id, product_id, quantity, create_time)
        VALUES (#{userId}, #{productId}, #{quantity}, datetime('now','localtime'))
    </insert>

    <update id="updateQuantity">
        UPDATE cart_item SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart_item WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM cart_item WHERE user_id = #{userId}
    </delete>
</mapper>
